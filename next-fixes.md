On server rundown on render all saved data dies, which is bad, need to make the ability to either SAVE all the data into some file that you can download and then import, or use supabase to store games which would mean architecture upgrading.

The videos take a long time to load and are really slow to load look at it, may just be a thing in this cluade in browser and not a real issue

A lot of time is spent on getting all the anime openings, 32 take around 16 seconds to get (not awful but kind of bad as at 256 it takes at least 2 whole minutes to laod all.)
